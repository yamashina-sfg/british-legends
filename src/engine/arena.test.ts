import{describe,expect,it}from'vitest';import{createNewSave}from'./save';import{applyArenaReward,ARENA_WAVE_LIMIT_MS,arenaRanking,arenaReward,arenaTimerTone,formatArenaTime,nearbyArenaRanking}from'./arena';
describe('闘技場',()=>{it('WAVE5初回は固有装備、2回目は周回報酬になる',()=>{let save=createNewSave(1);let first=applyArenaReward(save,5);expect(first.reward.equipmentId).toBe('iron_sword');let second=applyArenaReward(first.save,5);expect(second.reward.itemId).toBe('high_recovery_potion');expect(arenaReward(5,false).equipmentId).toBeUndefined();});it('自己記録を順位付けし周辺順位を抽出する',()=>{const rows=arenaRanking(3,'PLAYER',15000);const player=rows.find(row=>row.isPlayer)!;expect(player.rank).toBeGreaterThan(0);expect(nearbyArenaRanking(rows)).toContain(player);expect(rows.every((row,index)=>row.rank===index+1)).toBe(true)});it('5分制限を分秒表示し90秒と30秒で警告する',()=>{expect(ARENA_WAVE_LIMIT_MS).toBe(300_000);expect(formatArenaTime(300)).toBe('5:00');expect(formatArenaTime(89)).toBe('1:29');expect(arenaTimerTone(91)).toBe('normal');expect(arenaTimerTone(90)).toBe('warning');expect(arenaTimerTone(30)).toBe('danger')})});
