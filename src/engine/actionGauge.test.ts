import{describe,expect,it}from'vitest';import{ACTION_GAUGE_MAX,actionGaugeDurationMs,actionGaugePerSecond,actionGaugePercent}from'./actionGauge';describe('action gauge specification',()=>{it('uses max 300 and (199+SPD)/sec',()=>{expect(ACTION_GAUGE_MAX).toBe(300);expect(actionGaugePerSecond(101)).toBe(300);expect(actionGaugeDurationMs(101)).toBe(1000);expect(actionGaugePercent(500,101)).toBe(50)});it('never falls below base speed or exceeds 100%',()=>{expect(actionGaugePerSecond(-50)).toBe(199);expect(actionGaugePercent(99999,0)).toBe(100)})});
