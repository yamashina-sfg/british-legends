import{describe,expect,it}from'vitest';import{criticalChance,evasionChance,luckDropMultiplier,resolveLuckHit}from'./damage';
describe('latest LUK specification',()=>{it('calculates critical and capped evasion from LUK difference',()=>{expect(criticalChance(150,0)).toBe(.1);expect(evasionChance(0,150)).toBe(.1);expect(evasionChance(0,9999)).toBe(.95)});it('makes explosion attacks unavoidable while retaining critical checks',()=>{expect(resolveLuckHit(0,150,true,0,.5).evaded).toBe(false);expect(resolveLuckHit(150,0,false,.9,.05).critical).toBe(true)});it('caps drop influence at LUK 3600',()=>{expect(luckDropMultiplier(360)).toBe(2);expect(luckDropMultiplier(9999)).toBe(11)})});
